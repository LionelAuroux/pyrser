"""
This package purpose is to generate the code that will represent the grammar.

browse_grammar.py contains a class that will help to browse the grammar AST.
procedural.py contains a class that will help to generate some procedural language.
python.py contains a class that generates some python to represent grammars.
docstring.py contains a class that regenerates the DSL to help in debug/help.
helper.py contains a helper class that will ease the code generation.
"""
